# 作品集网站内容维护指南

这份文件用来说明：哪些是已经确定的设计规则，哪些地方可以安全修改，以及以后如何添加项目内容。

## 1. 先区分三层

不要每次都从头改样式。现在网站可以分成三层：

- 视觉规则层：颜色、字体、背景、卡片、弹窗、响应式规则，主要在 `index.html` 顶部 `<style>` 里。
- 内容结构层：首页文案、作品项目、工作经历、联系方式，主要在 `index.html` 的 `<body>` 里。
- 交互逻辑层：菜单、联系弹窗、作品区滚动聚焦、项目详情弹层，主要在 `index.html` 底部 `<script>` 里。

日常加内容，优先改“内容结构层”；不要随便改全局颜色和组件规则。

## 2. 页面文案原则

这个网站是对外展示页面，页面上只放访客需要看到的信息：

- 可以出现：项目名称、项目一句话、职责、能力、成果、联系方式。
- 不要出现：模块说明、制作说明、维护提示、占位说明、后续怎么填、为什么这样设计。

这些说明只写在这份维护文档里，不写进 `index.html` 的可见页面。

## 3. 首页内容怎么改

搜索这些关键词即可定位：

- 主标题：搜索 `hero-title`
- 能力标签：搜索 `hero-focus-tags`
- 联系按钮：搜索 `contact-trigger`
- 首页背景图：搜索 `--hero-bg-1`

当前首页主标题分成两个 `<span>`，桌面端保持两行，手机端允许三行。修改文字时尽量保留两个 `<span>` 的结构。

## 4. 作品项目怎么添加

作品区搜索：

```html
data-project-list
```

每个项目是一个：

```html
<article class="project-slide" data-project-title="项目标题" data-project-desc="项目一句话说明">
```

新增项目时：

1. 复制一个完整的 `.project-slide`
2. 修改 `data-project-title`
3. 修改 `data-project-desc`
4. 修改项目标题、简介、年份/类型、标签、要点和预览区文案
5. 如果需要不同预览色，可以给 `.module-preview` 增加一个新的类，并在 `<style>` 中补对应背景

作品区现在是纵向项目流：一屏聚焦一个项目，不再使用左右切换、圆点和右下角计数。

## 5. 项目详情怎么填图片

现在点击项目会打开项目详情弹层，里面有三个占位块：

```html
<div class="detail-image-slot">项目封面 / 总览图占位</div>
```

后续你准备好图片后，可以改成：

```html
<img class="detail-image" src="assets/projects/tax-cloud/01-overview.png" alt="税纪云项目总览图" />
```

建议图片放在：

```text
assets/projects/项目英文名/
```

推荐命名：

- `01-cover.png`
- `02-flow.png`
- `03-ui.png`
- `04-system.png`
- `05-review.png`

长图也可以直接放，`.detail-image` 已经有自适应宽度。

## 6. 联系方式怎么改

搜索：

```html
contactPopover
```

这里控制顶部邮箱 icon 和首页“联系我”按钮弹出的悬浮联系卡。

当前包含：

- 邮箱
- 手机号
- 微信二维码
- 微信号

二维码图片在：

```text
assets/visual/wechat-qr.png
```

页面底部联系区也有一份联系方式，搜索：

```html
id="contact"
```

如果改邮箱或手机号，建议两个地方一起改，避免信息不一致。

## 7. 哪些规则尽量不要随便改

这些已经是当前风格核心：

- 黑色科技蓝背景
- 首页数字山背景图
- 顶部集成式导航
- 联系方式悬浮卡
- 作品区纵向滚动、“一屏一个项目”
- 项目详情弹层
- 手机端响应式规则

如果要改这些，建议按“整体风格调整”来做，不要只改某一个局部颜色，否则页面会重新变得分裂。

## 8. 推荐添加内容顺序

1. 先补作品项目顺序和项目名称
2. 每个项目先写一句话：这个项目解决了什么问题
3. 给每个项目准备 3 到 5 张详情图片
4. 替换项目详情弹层里的占位块
5. 最后再微调每个项目卡片上的标签和要点
