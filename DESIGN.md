# 深圳国际科技信息中心案例页设计规范

> 基础方向来自 `awesome-design-md` 的 IBM 企业技术模板，并结合当前作品集的暖白画布、国科信品牌蓝和既有圆角体系重新约束。本文只适用于 `/portfolio/project/gkx/`、`/systems/` 与 `/design-system/`，不得影响其他项目。

## 产品语境

- 页面类型：大型 B 端平台项目案例，不是后台系统本体，也不是营销落地页。
- 阅读目标：招聘方应快速理解项目范围、我的职责、Codex 参与方式、设计规范和真实页面证据。
- 真实性边界：业务字段、数据、流程均需脱敏或重构；不虚构结果、指标、验收或客户反馈。
- 页面职责：`项目故事` 讲方法与决策，`系统实景` 展示运行页面，`设计规范` 展示协作规则和受限预览。

## 设计原则

1. **企业可信感优先**：以结构、对齐、克制蓝色和真实界面建立可信度，不依赖炫光、深色大屏或营销装饰。
2. **证据先于说明**：能用运行页面、关系网络、流程状态表达的内容，不再重复写成长段解释。
3. **AI 与设计判断分层**：明确 Codex 负责整理和生成，我负责范围、体验判断、规范与质量审核。
4. **复杂但可扫描**：页面可以信息丰富，但每一屏只允许一个主阅读路径和一个主要视觉锚点。
5. **组件服务叙事**：关系图、筛选器和卡片必须解释项目结构或工作方式，禁止纯装饰组件。

## 视觉语言

- 气质：浅色、专业、技术型、企业级，参考 IBM 的结构纪律，不照搬 Carbon 的全直角造型。
- 选中、操作和关键节点使用已确认的国科信项目主蓝；灰阶承担背景、正文和边界，警示色只用于保密或异常状态，避免整页泛蓝。
- 页面使用统一浅灰蓝画布，章节依靠留白和内容组件分隔，不交替切换大面积背景色。
- 产品截图放在白色或浅蓝舞台上，圆角 18–20px；仅主要证据容器允许柔和蓝色投影。
- 图标使用统一 SVG 图标；不使用 emoji、Unicode 图形或无意义装饰图标。

## 设计变量

### 色彩

| 角色 | 值 | 用途 |
| --- | --- | --- |
| Brand | `#165DFF` | 当前状态、主要操作、关键节点 |
| Brand Hover | `#4080FF` | Hover |
| Brand Active | `#0E42D2` | Pressed、强调文字 |
| Brand Light | `#E8F3FF` | 选中背景、AI 节点、关系簇背景 |
| Page | `#F7F8FA` | 全局统一画布 |
| Surface | `#FFFFFF` | 内容组件、截图与工作台 |
| Subtle | `#F0F2F5` | 次级面板、未选中状态 |
| Ink | `#222A35` | 标题和主要正文 |
| Muted | `#5C6673` | 解释文字 |
| Faint | `#8A94A3` | 辅助信息 |
| Line | `#DEE4EB` | 必要边界，不用于装饰性分割 |

### 字号

- 字体：`Inter`, `PingFang SC`, `Microsoft YaHei`, sans-serif。
- 案例标题：52–70px，400–500 weight，最多两行。
- 子页标题：46–76px，400 weight。
- 章节标题：30–36px，480–500 weight。
- 正文：16px / 1.8。
- 标签与按钮：14px。
- 辅助文字：12–13px；任何可见文字不得低于 12px。
- 禁止重复使用章节眉题、编号小字和英文装饰标签。

### 间距、圆角与深度

- 基础间距：4px；常用间距为 8 / 12 / 16 / 24 / 32 / 48 / 64 / 92px。
- 内容最大宽度：1200px；桌面左右安全边距 24px，移动端 16px。
- 控件圆角：8–10px；内容组件：14–20px；总结面板：24px。
- 阴影只用于主截图、交互工作台和一级浮层，统一使用蓝色低透明度的向下柔和投影。

## 页面布局

- 全局章节定位器固定在 1200px 内容区外侧，沿用 `zhaocai-locator`。
- 主页面不得连续使用同一种左右分栏：需求范围上下展开，原型验证左右分栏，系统落地全宽截图，规范协作居中总结。
- 桌面章节上下间距约 92px；移动端约 70px。
- 正文阅读宽度控制在 45–75 个中文字符内。

## 核心组件

### 项目页签

- 三等分：项目故事 / 系统实景 / 设计规范。
- 桌面宽度约 390px，单项高度 48px；移动端占满容器。
- 当前页使用实心品牌蓝，Hover 使用浅蓝背景，Focus 必须有清晰轮廓。

### 产品体验覆盖关系网络

- 目的：解释统一设计语言如何覆盖不同任务，不作为装饰性关系图。
- 语义分组固定为三类：框架与导航、数据与反馈、表单与选择。
- 桌面主画布高度 500–560px；中心节点直径约 176px，一级节点约 132px，二级节点约 108–116px。
- 节点簇必须铺满画布并保持清晰疏密；禁止所有节点缩成同尺寸的小圆点。
- 连接线必须表达从中心规则到任务簇的覆盖关系；SVG 只承担关系，节点按钮承担文本和交互语义。
- 筛选项：全部 / 框架与导航 / 数据与反馈 / 表单与选择。按钮使用 `aria-pressed`，节点使用真实 `button`。
- 点击筛选后，匹配簇提高对比度，非匹配簇降低透明度；选择节点后展示节点职责、使用场景和与其他节点的关系。
- 关系网络下方必须有 14–16px 的说明区，明确它解释的是跨系统统一规则，而非业务架构或真实数据拓扑。
- 小屏采用可横向查看的稳定画布或分组列表，不允许压缩到无法阅读。

### 原型工作台

- 四阶段：输入、Codex 生成、设计判断、评审版本。
- Codex 节点使用品牌蓝，其余节点使用白色；阶段文字不得低于 12px。
- 原型切换按钮最小高度 44px，切换后图片、标题和说明同步更新。

### AI 协作工作流

- 用于说明 AI 如何进入真实设计过程，不展示工具清单或虚构效率指标。
- 内容固定分为 `AI Loop` 与 `Human Loop`：AI 负责材料拆解、结构草案、可运行原型和规范整理；设计师负责事实核对、业务边界、体验判断与最终质量。
- 工作流按业务上下文、结构化 Spec、Codex 探索、可运行原型、设计收敛、规范沉淀六步表达，并明确“预览—评审—修正—再次验证”的循环。
- 继续使用国科信浅色画布与项目主蓝，不复制参考案例的深色背景、业务数据和效果指标。

### 系统实景

- 桌面使用左侧页面选择器 + 右侧主截图；移动端切换为横向可滑动标签。
- 活跃页面使用白色高对比状态，其他页面保持浅蓝底。
- 补充页面可采用双列图片卡，移动端变为水平滑动，禁止堆叠成超长单列。

### 设计规范预览

- 预览必须使用真实可操作页面，不用静态截图替代。
- 左侧目录可切换；受保密限制的工作区只露出标题和开头，并使用白色渐变遮罩。
- 遮罩说明使用正文级字号，不能用模糊小字暗示保密。

### 跳转与结果组件

- 跳转条是明确的真实链接，左侧说明目标，右侧显示品牌蓝“查看”动作。
- 结果区以一块浅蓝总结面板收束，不使用深色背景或虚构指标。

## 交互状态

- 所有按钮必须覆盖 Default / Hover / Focus / Active / Disabled。
- 交互过渡 160–220ms，使用 ease-out；页面加载时保持默认内容可见。
- 图片和非交互内容不得添加假 Hover 或手型光标。
- 选中、加载、空、错误状态必须保持组件最终布局尺寸，避免跳动。
- 尊重 `prefers-reduced-motion`，移除位移动画但保留状态变化。

## 响应式

- `>1120px`：1200px 主内容，定位器在外侧；关系网络使用完整画布。
- `821–1120px`：主叙事转单列，关系网络缩小但保持中心/一级/二级层级。
- `<=820px`：系统页签横向滚动，图片说明单列，关系网络允许横向查看。
- `<=560px`：页面边距 16px；标题、正文、图片和元信息按阅读顺序堆叠；触控目标不低于 44px。
- 任意断点不得产生页面级横向溢出。

## 可访问性

- 正文与背景对比度至少 4.5:1，大字号至少 3:1。
- 键盘顺序与视觉顺序一致；筛选器和节点必须可用 Tab、Enter 和 Space 操作。
- 图片提供具体的页面说明型 alt，不使用“图片”“截图”等空泛文案。
- 关系线 `aria-hidden`，关系含义由节点文本、选中状态和说明区表达。

## 文案语气

- 使用专业 UX 设计师的事实陈述：背景 → 判断 → 动作 → 证据。
- 不使用“赋能、重塑、全链路闭环、显著提升”等空泛结论。
- 缺少真实指标时不补写结果；需要用户提供的数据标记为待确认。

## 页面生成规则

1. 修改国科信页面前先读取本文和现有组件实现。
2. 复用 `NationalSciencePlatformCase.tsx` 与 `national-science-platform.css` 的既有类和组件。
3. 新组件必须服务现有叙事，不改变其他项目的 CSS 和数据。
4. 先验证桌面 1567px 与移动 390px，再运行构建。
5. 发布前检查资源加载、控制台、字号下限、页面溢出、键盘状态和返回位置。

## 禁止

- 深色大背景、玻璃拟态、霓虹渐变和装饰性网格。
- 无意义卡片套卡片、重复左右分栏、过多分割线。
- 仅用小字号蓝色标签建立层级。
- 把真实界面缩成不可读的装饰缩略图。
- 与业务无关的图标、节点、指标或排名。
- 修改招财 Smart 或其他作品集项目的布局与样式。
